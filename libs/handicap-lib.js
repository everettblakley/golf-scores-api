export function calculateHandicap(scores) {
    // The Players Condistions Calculations (PCC) determines the condition
    // of play for the day, and ranges from -1.0 to +3.0
    // We will use a PCC of 0 because fuck that non-sense
    // const PCC = 0;

    // let differentials;

    // differentials = scores.map(score => {
    //     return score.holes === 18
    //     ? 113 / score.slop * (score.grossScore - score.rating - PCC)
    //     : ???
    // })

    return 0;
};